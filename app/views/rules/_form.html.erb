
<%= form_for @rule, :html => { :class => 'form-horizontal' } do |f| %>

    <div class="control-group">
      <%= f.label :field, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :field, :class => 'text_field' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :operator, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :operator, :class => 'text_field' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :value, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :value, :class => 'text_field' %>
      </div>
    </div>
    <%= f.hidden_field  :alert_id %>
    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  @rule.alert, :class => 'btn' %>
    </div>
<% end %>
